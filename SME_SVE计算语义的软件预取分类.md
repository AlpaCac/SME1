总结了在SME/SVE层面可能的计算形式，并据此对可行的预取方案进行了分类。

## 1. SME/SVE 计算形式分类

| 类别 | 预取对象 | 推荐距离单位 |
|---|---|---|
| SVE 连续逐元素计算 | 后续连续向量载入的数据 | 向量迭代、缓存行 |
| SVE 索引/Gather 计算 | 索引流和离散数据流 | 索引批次、向量迭代 |
| SVE 归约 | 连续输入流 | 归约展开步、向量迭代 |
| SME 外积计算 | 将被装入 Z 寄存器的 A/B 操作数 | 外积步、K 步 |
| SME ZA Tile 装载/恢复 | ZA 的 backing memory | Tile、Tile 行片段 |
| SME ZA 结果写回 | 结果目的地址 | Tile、存储批次 |

## 2. 各类预取的详细说明

### 2.1 SVE 连续逐元素计算输入预取

SVE 连续逐元素计算以向量长度为步长遍历数组。每次迭代处理一组连续元素，各 lane 对应相邻的内存位置，并分别完成加、乘、FMA、比较或类型转换等计算。除循环尾部外，各 lane 通常相互独立；尾部通过谓词只激活仍处于合法范围内的元素。

`lane` 指一条 SVE 向量中的一个元素位置，中文通常称为“向量通道”或“向量元素槽位”。例如：

```
Z 寄存器：  [10.0, 20.0, 30.0, 40.0]
lane 编号：    0     1     2     3
```

#### 应如何预取

预取目标应是当前迭代之后第 `D` 个向量迭代将访问的数据：

具体策略如下：

1. 规则连续地址通常使用标量地址预取，不必因为消费者是 SVE 就使用 Gather 预取。
2. 预取地址按缓存行对齐或去重，避免相邻迭代反复预取同一缓存行。
4. 数据在短期内还会复用时选择 `KEEP`；单次、长距离顺序扫描可选择 `STRM`。
5. 在循环尾部，只预取能够证明处于合法对象范围内的地址，或将预取限制在无尾部的主循环中。

### 2.2 SVE 索引/Gather 计算输入预取

SVE 索引/Gather 计算不是按相邻地址读取数据，而是每个活跃 lane 先取得一个索引，再由该索引计算实际数据地址。即间接访存。

因此，计算同时包含两部分：按顺序读取索引，以及根据索引访问离散数据。只有当未来索引已经可用时，才能准确得到未来的离散数据地址。

#### 应如何预取

这类计算适合采用两阶段预取：

1. 提前预取或加载未来索引批次；
2. 索引向量可用后，根据未来索引对离散数据执行 Gather 预取。

具体策略如下：

1. 对索引数组本身，按普通连续流预取。
2. 对索引指向的数据，在多个活跃 lane 地址同时可用时采用 SVE Gather 预取。
5. 地址高度随机、复用很低时倾向 `STRM`；热点索引或短期重复访问时倾向 `KEEP`。

### 2.3 SVE 归约输入预取

SVE 归约把一段连续输入压缩为一个或少量结果，例如求和、最大值、最小值或点积。每次向量迭代读取一组连续元素，先在一个或多个向量累加器中形成部分结果，最后再完成横向归约。

假设一个 SVE 向量一次可以处理 `VL` 个 `float`：

```
第 0 次：读取 a[0 ... VL-1]
第 1 次：读取 a[VL ... 2VL-1]
第 2 次：读取 a[2VL ... 3VL-1]
```

每次读取的向量先累加到向量累加器中。循环结束后，再把累加器中各个位置的部分和合并为一个标量结果。

#### 应如何预取

具体策略如下：

1. 归约输入通常是规则连续流，优先使用标量地址预取。
2. 输入只消费一次且工作集较大时，优先考虑 `STRM`，减少对其他数据的缓存干扰。
3. 如果同一输入片段还参与其他计算或多次归约，可以改用 `KEEP`。
4. 增大循环展开会增加每步计算量，因此应重新计算预取距离，不能沿用未展开循环的参数。
5. 当归约已经受计算依赖而不是内存延迟限制时，增加软件预取可能没有收益，应由成本模型关闭。
6. 预取请求按缓存行去重，并避开不能证明地址合法的尾部区域。

### 2.4 SME 外积操作数预取

SME 外积计算把两个向量分别视为行、列方向的操作数，并将它们的外积累加到 ZA Tile。以矩阵分块计算为例，在每个 K 步中：

1. 读取 A 的一个向量片段；
2. 读取 B 的一个向量片段；
3. 计算两个向量的外积；
4. 将外积结果累加到 ZA；
5. 沿 K 维重复，直到当前输出 Tile 完成。

若当前 Tile 的 K 范围为 `Ktile`，则一个输出 Tile 需要执行约 `Ktile` 个外积步。A、B 操作数可能具有不同复用方式：一个 A 片段可能服务多个 N 方向 Tile，一个 B 片段也可能服务多个 M 方向 Tile。

#### 应如何预取

预取对象是未来第 `D` 个 K 步将装入 Z 寄存器的 A、B 源数据.

具体策略如下：

1. 预取距离以 K 步或外积步表示，再根据 A、B 的布局换算为实际字节地址。
2. A、B 为规则连续或规则步长布局时，通常使用标量地址预取；只有稀疏或索引化操作数才考虑 Gather 预取。
3. A、B 的复用次数和访问跨度可能不同，应分别决定预取距离、缓存层级和 `KEEP/STRM`，不能强制使用同一策略。
4. 能被当前或相邻输出 Tile 多次使用的操作数优先选择 `KEEP`；打包后只顺序消费一次的数据可选择 `STRM`。
5. 大矩阵块可采用两级接力：较远位置预热到 L2，接近使用时再预取到 L1。

### 2.5 SME ZA Tile 装载/恢复预取

ZA Tile 装载/恢复出现在已有矩阵状态需要从内存读回时，例如读取初始输出 Tile、恢复部分累加结果，或从 backing memory 恢复 ZA 内容。计算过程通常为：

1. 按水平或垂直 Tile 切片读取内存；
2. 将各切片恢复到 ZA 的相应位置；
3. 在恢复完成后继续执行外积累加、后处理或其他 Tile 计算。

一个 ZA Tile 的大小与流式向量长度和元素类型有关，通常跨越多条缓存行。因此，应根据实际 Tile 切片布局计算覆盖范围，而不能把整个 Tile 当作单一缓存行。

#### 应如何预取

仅当程序中确实存在 ZA backing-memory 读取时才生成这类预取。预取地址根据未来 Tile 或未来 Tile 切片计算：

具体策略如下：

1. 在开始恢复当前 Tile 前，预取该 Tile 即将读取的缓存行；连续处理多个 Tile 时，可预取下一 Tile。
2. 预取覆盖范围按 Tile 切片和缓存行展开，并对重叠缓存行去重。
3. Tile 装入后通常会参与一段计算，因此近距离预取一般倾向 L1 + `KEEP`。
4. Tile 较大或恢复时点较远时，可以先预热到 L2，再在接近加载时预取到 L1。
6. 如果 ZA 在进入计算前被直接清零而不是从内存恢复，则不需要此类读预取。

### 2.6 SME ZA 结果写回预取

当 K 方向的外积累加完成后，ZA 中保存一个输出 Tile。结果写回通常按 ZA 的水平或垂直切片逐步存入目标矩阵。若存在激活、缩放、偏置或与原结果合并等后处理，应先完成对应 Tile 计算，再写出最终结果。

一个输出 Tile 同样跨越多条缓存行。是否需要为写回准备缓存状态，取决于目标地址是否已在缓存中、处理器的写分配策略，以及结果在写回后是否会很快被再次读取。

#### 应如何预取

这类预取针对未来将被写入的结果地址，距离以输出 Tile或存储批次表示。

具体策略如下：

1. 如果写缺失会触发明显的写分配开销，可在写回前对未来输出 Tile 使用写预取。
2. 结果写回后很快会被后续阶段读取时，优先选择较近缓存层级和 `KEEP`。
3. 大规模结果只写一次且短期不再使用时，可考虑 `STRM` 或非临时存储；非临时存储是实际存储策略，不等同于写预取。
4. 如果输出 Tile 在计算前已经被读取，例如需要与原 C Tile 合并，则对应缓存行可能已经驻留，不应重复执行写预取。
6. 写预取效果高度依赖具体微架构，应默认作为可选策略，通过性能计数器和基准测试决定是否启用。





