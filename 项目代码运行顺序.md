# 项目代码运行顺序

本文说明 SME1 项目从原始 C kernel 到预取汇编、正确性报告和性能报告的
实际运行顺序。所有命令默认从仓库根目录 `SME1` 执行。

## 一、整体依赖关系

项目的编译与验证链为：

```text
stencil_sme_kernels.c
        |
        v
步骤 1：Clang 生成 LLVM IR 并检查结构
        |
        v
01_llvm_ir_analysis/output/stencil_sme_kernels.ll
        |
        v
步骤 2：构建并加载 StencilPrefetchPass
        |
        v
步骤 3：识别 2D5P/3D7P 和物理访存流
        |
        v
步骤 4：完成预取决策并插入 llvm.aarch64.prefetch
        |
        v
02_llvm_pass_plugin/output/*.ll 和 *.s
        |
        v
步骤 5：Apple M5 正确性、性能、PMU 和多线程验证
        |
        v
05_runtime_validation/output/ 中的报告
```

步骤 4 已经完成软件预取插入。步骤 5 不再修改 LLVM IR，只负责验证步骤 4
生成的基线和预取程序。

## 二、运行前准备

进入仓库根目录：

```bash
cd /path/to/SME1
```

步骤 1 默认使用系统 Clang。步骤 2～4 需要与插件 ABI 匹配的 LLVM 18、
CMake 和 Ninja。仓库脚本默认从 SME 工作区查找：

```text
../external/Polygeist/build/bin/llvm-config
../toolchains/CMake.app/Contents/bin/cmake
../downloads/ninja
```

如果工具不在默认位置，可以通过 `LLVM_CONFIG`、`LLVM_CLANG`、`CMAKE`、
`NINJA` 和 `PLUGIN_CXX` 覆盖。Apple M5 的实际运行阶段使用系统 Clang
组装 `+nosve+sme` 汇编。

## 三、最小完整运行顺序

### 1. 生成并检查 LLVM IR

运行：

```bash
./01_llvm_ir_analysis/generate_and_check.sh
```

输入：

```text
stencil_sme_kernels.c
```

主要输出：

```text
01_llvm_ir_analysis/output/stencil_sme_kernels.ll
01_llvm_ir_analysis/output/analysis_report.md
```

这一阶段检查两个函数是否保留后续 pass 需要的信息，包括：

1. SME locally-streaming 函数属性。
2. `cntsw` 向量步长和循环归纳变量。
3. `getelementptr` 地址计算。
4. 2D5P 的 5 个 masked load。
5. 3D7P 的 7 个 masked load。
6. masked store 和 SVE 浮点计算。

步骤 2～4 读取这里生成的 LLVM IR，因此这是整个流程的第一个必跑步骤。

### 2. 构建 LLVM pass 并完成步骤 2～4

运行：

```bash
./02_llvm_pass_plugin/build_and_test.sh
```

该脚本依次执行：

1. 使用 CMake/Ninja 构建 `StencilPrefetchPass` 动态插件。
2. 使用 LLVM 18 Clang 加载插件。
3. 获取 LoopInfo、ScalarEvolution 和 DominatorTree 等分析。
4. 识别 2D5P/3D7P 及 current、row、plane 物理流。
5. 使用 `generic-sme` Profile 运行默认决策回归。
6. 运行负例、预算拒绝、短循环和幂等测试。
7. 生成无插件基线汇编和带预取汇编。
8. 额外生成固定的 `apple-m5` Profile 汇编。
9. 检查 `llvm.aarch64.prefetch` 最终降为 `PRFM/PRFUM`。

主要输出：

```text
02_llvm_pass_plugin/build/StencilPrefetchPass.dylib
02_llvm_pass_plugin/output/plugin_test_report.md
02_llvm_pass_plugin/output/stencil_recognition_report.md
02_llvm_pass_plugin/output/stencil_prefetch_decision_report.md
02_llvm_pass_plugin/output/stencil_sme_kernels.ir-baseline.s
02_llvm_pass_plugin/output/stencil_sme_kernels.s
02_llvm_pass_plugin/output/stencil_sme_kernels.apple-m5.s
```

其中：

```text
stencil_sme_kernels.ir-baseline.s
  同一输入 IR、同一优化管线，但不运行预取 pass

stencil_sme_kernels.s
  generic-sme Profile 生成的预取汇编

stencil_sme_kernels.apple-m5.s
  Apple M5 Profile 生成的预取汇编
```

### 3. 运行 Apple M5 数值正确性测试

运行：

```bash
SME_RUNTIME_PROFILE=apple-m5 FORCE_SME_RUN=1 \
  ./05_runtime_validation/build_and_run.sh
```

该脚本会再次调用步骤 2～4 的构建测试，确保汇编是最新的，然后：

1. 用 `-march=armv9.2-a+nosve+sme` 组装 SME kernel。
2. 用普通 arm64 目标编译测试驱动。
3. 分别链接基线和预取版本。
4. 调用 `svcntb()` 测量实际 streaming VL。
5. 将两个版本与标量 2D5P/3D7P 参考实现比较。
6. 在前后 guard page 附近运行边界和谓词尾部测试。

主要输出：

```text
05_runtime_validation/output/runtime_info.log
05_runtime_validation/output/baseline_run.log
05_runtime_validation/output/prefetch_run.log
05_runtime_validation/output/correctness_report.md
```

只有 `correctness_report.md` 显示 `PASS` 后，才能继续解释性能结果。

### 4. 运行单进程配对性能测试

运行：

```bash
SME_RUNTIME_PROFILE=apple-m5 FORCE_SME_RUN=1 \
  ./05_runtime_validation/run_paired_benchmark.sh
```

该脚本将基线和预取 kernel 重命名后链接到同一可执行文件。奇偶样本交换
执行顺序，并计算每对样本的 `baseline_time / prefetch_time`，用于降低
进程启动、温度和调度漂移。

主要输出：

```text
05_runtime_validation/output/paired_2d_round_*.log
05_runtime_validation/output/paired_3d_round_*.log
05_runtime_validation/output/paired_benchmark_report.md
```

这是当前判断 2D/3D 单线程预取收益的主要性能入口。

## 四、步骤 5 的扩展实验顺序

以下实验不是生成预取汇编的必经步骤。需要重新调优 Profile 时，建议按
下面的顺序执行。

### 1. 初始独立进程墙钟基线

```bash
SME_RUNTIME_PROFILE=apple-m5 FORCE_SME_RUN=1 \
  ./05_runtime_validation/run_benchmark.sh
```

输出：

```text
05_runtime_validation/output/benchmark_report.md
```

独立进程结果容易受到系统状态影响，只用于建立基线，最终判断优先使用
配对测试。

### 2. 扫描预取距离和问题尺寸

```bash
FORCE_SME_RUN=1 ./05_runtime_validation/run_profile_sweep.sh
```

输出：

```text
05_runtime_validation/output/profile_sweep_report.md
```

该脚本扫描：

1. 2D row-L1 KEEP 距离 `1/2/4/8`。
2. 3D plane-L1 STRM 距离 `1/2/4/8`。
3. 不同 row 字节数和 plane 大小。

### 3. 运行预取类别消融

```bash
FORCE_SME_RUN=1 ./05_runtime_validation/run_ablation.sh
```

输出：

```text
05_runtime_validation/output/ablation_report.md
```

该实验分别关闭或只保留 row、plane-L1、plane-L2，判断性能变化来自哪一
类预取。

### 4. 采集 Apple M5 CPU Counters

先在 Instruments 中准备 `SME Stencil Cache Counters` 手动模板，再运行：

```bash
FORCE_SME_RUN=1 \
  ./05_runtime_validation/collect_cpu_counters.sh baseline 3d
FORCE_SME_RUN=1 \
  ./05_runtime_validation/collect_cpu_counters.sh prefetch 3d
FORCE_SME_RUN=1 \
  ./05_runtime_validation/run_cpu_counter_comparison.sh 3d
```

输出：

```text
05_runtime_validation/output/pmu_comparison_report.md
05_runtime_validation/output/pmu_*/
```

`pmu_*` 保存 Instruments trace 和 XML，体积较大，只在本机保留。汇总报告
记录 L1D 和 PL2 事件的相对变化。

### 5. 运行多线程带宽竞争测试

```bash
FORCE_SME_RUN=1 ./05_runtime_validation/run_threaded_benchmark.sh
```

输出：

```text
05_runtime_validation/output/threaded_benchmark_report.md
```

该实验用 1/2/4/8 个线程处理独立 3D7P 网格，检查额外预取流量在共享
cache 和内存带宽竞争下是否仍然有效。

## 五、两个 Profile 环境变量的区别

直接调用 Clang 编译并加载 pass 时，使用：

```bash
SME_PREFETCH_PROFILE=apple-m5 clang \
  -fpass-plugin=./02_llvm_pass_plugin/build/StencilPrefetchPass.dylib ...
```

`SME_PREFETCH_PROFILE` 由 LLVM pass 读取，决定本次编译使用哪套预取参数。

步骤 5 的脚本使用：

```bash
SME_RUNTIME_PROFILE=apple-m5 \
  ./05_runtime_validation/build_and_run.sh
```

`SME_RUNTIME_PROFILE` 不重新计算决策，只决定链接步骤 2～4 已生成的哪一份
汇编：

```text
generic-sme -> stencil_sme_kernels.s
apple-m5   -> stencil_sme_kernels.apple-m5.s
```

## 六、最常用的完整命令

只验证当前实现：

```bash
./01_llvm_ir_analysis/generate_and_check.sh
./02_llvm_pass_plugin/build_and_test.sh
SME_RUNTIME_PROFILE=apple-m5 FORCE_SME_RUN=1 \
  ./05_runtime_validation/build_and_run.sh
SME_RUNTIME_PROFILE=apple-m5 FORCE_SME_RUN=1 \
  ./05_runtime_validation/run_paired_benchmark.sh
```

重新进行完整调优：

```bash
./01_llvm_ir_analysis/generate_and_check.sh
./02_llvm_pass_plugin/build_and_test.sh
SME_RUNTIME_PROFILE=apple-m5 FORCE_SME_RUN=1 \
  ./05_runtime_validation/build_and_run.sh
FORCE_SME_RUN=1 ./05_runtime_validation/run_profile_sweep.sh
FORCE_SME_RUN=1 ./05_runtime_validation/run_ablation.sh
SME_RUNTIME_PROFILE=apple-m5 FORCE_SME_RUN=1 \
  ./05_runtime_validation/run_paired_benchmark.sh
FORCE_SME_RUN=1 \
  ./05_runtime_validation/run_cpu_counter_comparison.sh 3d
FORCE_SME_RUN=1 ./05_runtime_validation/run_threaded_benchmark.sh
```

## 七、验收顺序

每次修改代码后，按以下顺序判断：

1. `analysis_report.md` 必须通过，证明 LLVM IR仍可分析。
2. 步骤 2～4 的三个报告必须通过，证明插件、识别和插入正确。
3. `correctness_report.md` 必须为 `PASS`。
4. 汇编中预取数量、层级和策略必须符合所选 Profile。
5. 基线与预取 checksum 必须一致。
6. 最后才能使用配对墙钟和 PMU 数据评价性能。

如果前一项失败，不应继续解释后一项的性能结果。
